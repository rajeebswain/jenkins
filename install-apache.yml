---
- hosts: web
  become: true
  tasks:
    - name: stop apache
      service:
        name: apache2
        state: started
    - name: deploy static website
      copy:
        src: index.html
        dest: /var/www/html
